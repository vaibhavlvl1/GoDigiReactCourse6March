import React from "react";
import Pagination from "@mui/material/Pagination";

export default function Buttons({ entries, prodLength }) {
  const pages = Math.ceil(prodLength / entries);
 
  return ;
}
